# AI UI Design Workflow

## 1. 文档定位

这份文档记录当前已经确认的 AI UI 设计工作流，以及 Dockyard（插件停靠坞）作为中心管理工具时需要承载的交互和数据边界。

当前决定是：Dockyard 不只是工作流的服务对象，而是这套工作流的管理中心。它负责组织设计资料、调用模型和 Skill、生成 HTML 审查界面、接收用户取舍并保存结构化结果。正式项目代码仍然保存在用户选择的代码仓库中。

## 2. 核心目标

- 用户负责产品结构、线框、关键判断和最终取舍。
- 模型负责生成 UI、提出修改方案、实现局部调整和整理变更。
- 设计规则和 Token（设计变量）从生成开始就进入上下文，但不预先规定具体视觉答案。
- 初版先生成完整 UI，后续修改以局部问题为单位，不反复重做整页。
- 视觉选择通过真实 HTML（网页）预览完成，文字只负责解释和记录。
- 用户确认后的选择沉淀为项目 Token、组件映射和设计决策。

## 3. 工作流输入

### 3.1 线框和功能上下文

在正式 UI 生成前，需要先确认：

- 页面主要区域和信息层级；
- 用户主流程和重要状态；
- 哪些结构已经冻结；
- 页面中的关键数据和操作；
- 需要实现的空状态、加载状态、错误状态和完成状态。

线框阶段只解决结构和流程，不解决正式视觉风格。

### 3.2 `DESIGN.md`（设计规则文档）

`DESIGN.md` 描述判断原则，而不是一份固定的颜色表。它可以包括：

- 产品类型和使用场景；
- 视觉气质和信息密度；
- 字体使用原则；
- 颜色使用原则；
- 组件和表面层级原则；
- 交互反馈原则；
- 动效原则；
- 无障碍和响应式要求；
- 明确禁止的问题，例如无意义的渐变、重复卡片或过度动画。

### 3.3 Token Schema（Token 结构定义）

全局 Token 只规定需要管理的内容范围和记录方式，不预先规定所有具体值。

基础类别包括：

- `color`（颜色）；
- `typography`（字体和文字层级）；
- `spacing`（间距）；
- `sizing`（尺寸）；
- `shape`（圆角和边框）；
- `motion`（动效参数）；
- `interaction`（交互状态）。

全局 Token 的作用是让不同项目都能用同一套分类和记录方式。具体的颜色、字号、间距和动效值由项目和设计过程产生。

### 3.4 项目 Token 状态

项目 Token 保存当前项目实际使用的值。例如：

```text
color.panel.background = #151e1f
color.button.primary = #63f4e2
typography.title.size = 20px
spacing.panel.padding = 16px
```

新项目可以从空状态开始。模型在生成初版 UI 时建立第一份项目 Token 状态，但这不是需要用户逐项审核的提案。

### 3.5 每轮任务 Prompt（提示词）

用户每轮只需要说明本次目标，例如：

```text
只调整组件检索页的静态视觉。
保留当前页面结构。
主按钮太亮，希望更克制一些。
不要修改画布和右侧检查器。
```

用户不需要预先知道具体 Token 名称。模型根据自然语言定位相关组件和 Token。

## 4. Token、布局和交互的管理边界

### 4.1 Token

Token 记录可复用的视觉和交互变量。项目可以动态产生新的 Token，系统需要记录它们的名称、值、影响范围和来源。

### 4.2 布局

页面具体结构属于项目内部布局，不设为所有项目共用的固定布局。

全局可以提供通用布局原语，例如：

- `stack`（纵向堆叠）；
- `cluster`（横向组合）；
- `grid`（网格）；
- `split-view`（分栏）；
- `sidebar`（侧栏）。

项目再定义自己的布局模式，例如 Dockyard 的工作区、图稿编辑器和组件检索页。布局和 Token 在概念上区分，但可以由同一个设计系统控制台统一展示。

### 4.3 交互

全局交互包括 `hover`（悬停）、`focus`（焦点）、`active`（激活）、`disabled`（禁用）、`loading`（加载）、`error`（错误）、键盘操作和减少动态等基础状态。

项目特有的流程，例如组件检索、拖入画布和生成上下文包，属于项目流程或状态机，不强行抽成全局 Token。

## 5. 正式 UI 工作流

### 阶段 A：确认完整线框

用户先确认整个页面的结构、信息层级和功能流程。此时不要求模型设计正式视觉。

### 阶段 B：生成整页初版 UI

模型接收：

```text
完整线框
+ DESIGN.md
+ Token Schema
+ 当前项目 Token
+ 本轮风格 Prompt
```

模型生成一版完整静态 UI。这个阶段：

- 不按左侧、中央、右侧分别生成方案；
- 不要求每个区域都产生 Token 提案；
- 不默认生成多个版本；
- 先建立一个完整、可运行、可观察的视觉基线。

模型需要在生成过程中使用或创建项目 Token，但系统先把它们记录为初始状态，不要求用户逐项审核。

### 阶段 C：用户指出局部问题

用户观察整页初版后，用自然语言指出问题。问题可以是颜色、字体、间距、组件细节、信息层级或交互反馈。

例如：

```text
组件检索页的主按钮太亮，希望更克制，但保留它的强调作用。
```

### 阶段 D：生成局部修改候选

模型根据当前 UI、项目 Token 和用户反馈：

1. 定位受影响的区域和组件；
2. 找到对应 Token；
3. 只生成相关的修改候选；
4. 保留未授权区域；
5. 输出变更说明。

默认只生成一个针对性候选。只有用户要求比较多个方向时，才生成 A、B、C 等多个方案。

### 阶段 E：HTML 审查和取舍

候选不直接覆盖正式 UI，而是生成一个临时 HTML 审查产物。它不是简单截图，而是尽量接近真实产品的可操作展示。

静态 UI 审查卡片至少包含：

- 当前版本；
- 建议版本；
- 变更区域；
- Token 原值和新值；
- 受影响组件；
- 未修改区域；
- `Approve`（接受）、`Deny`（拒绝）、`Discuss`（讨论）操作；
- 用户补充反馈入口。

用户可以接受整条建议，也可以要求模型保留其中一部分，再生成下一版候选。

### 阶段 F：保存确认结果

用户确认后，系统才保存：

- 项目 Token 变更；
- 组件和 Token 的映射；
- 设计决策记录；
- 当前版本和历史版本；
- 用户的补充说明。

模型发现的新 Token 先属于当前项目。只有它在多个项目中反复出现时，才提出是否提升为通用结构的建议。

## 6. 动效和交互阶段

静态 UI 稳定后，再进入 Emil Kowalski 的动效和交互 Skill（技能）阶段。

模型接收：

```text
已确认的静态 UI
+ DESIGN.md
+ 当前项目 Token
+ 本轮动效目标
+ Emil 的 Skill
```

动效候选也通过 HTML 审查产物比较：

- 当前动画；
- 建议动画；
- 播放、暂停或拖动控制；
- 时长、缓动和位移等参数；
- 减少动态方案。

例如：

```text
motion.candidate-drop.duration
180ms -> 220ms
```

动效只针对用户指定的交互，不重新设计已经确认的静态 UI。

## 7. Skill 的角色

### Jakub Krehel 的 Skill

主要用于静态 UI 阶段和静态审查：

- 布局；
- 字体；
- 颜色；
- 组件细节；
- 无障碍；
- 多方案比较；
- 综合界面审查。

### Emil Kowalski 的 Skill

主要用于动效和交互阶段：

- 动画取舍；
- 动效时长和缓动；
- 进入、退出和状态切换；
- 微交互；
- 动效性能和减少动态。

这些 Skill 负责提出和实现设计方案，不负责独立决定哪些变量属于全局、项目或组件范围。Token 层级、变更记录和批准流程由外层工作流管理。

## 8. 模型和系统的职责

### 模型负责

- 读取线框、规则、Token 和 Prompt；
- 生成完整 UI 初版；
- 根据反馈定位受影响区域；
- 生成局部修改候选；
- 在生成时整理项目 Token；
- 说明设计理由和影响范围；
- 为静态 UI 或动效生成 HTML 审查内容。

### 系统负责

- 组装上下文并调用模型和 Skill；
- 运行真实 UI 或隔离预览；
- 验证 Token 结构；
- 计算当前版本和候选版本的差异；
- 展示 HTML 审查产物；
- 管理接受、拒绝和讨论状态；
- 只有在用户确认后写入正式项目文件；
- 保存 Token、组件映射、设计决策和历史版本。

模型同步提出 Token，系统负责验证、展示、比较和保存。系统不需要在生成结束后重新猜测模型使用了哪些值。

## 9. 审查产物的形式

HTML 审查产物是 Dockyard 在每轮审查中生成的临时视觉对话界面，不是正式产品 UI。它可以在 Dockyard 内打开、交互和关闭，默认不要求把所有候选媒体永久保存。

它可以包含：

- 当前和建议的真实渲染；
- 多个方案切换；
- 可播放的动效；
- Token 差异；
- 组件影响范围；
- 用户批准状态；
- 一键生成给模型的反馈文本。

审查结果最终应转换为结构化记录，而不是只保留一段复制回对话的 Prompt。沿途截图、动效视频和被拒绝的候选默认只存在于临时目录；它们不是工作流完成所必需的长期数据。

## 10. 记录和交付物

每轮设计至少产生以下结构化记录：

```text
baseline/
  初始项目 Token
  初版来源和生成 Prompt

review/
  审查编号
  审查目标和影响范围
  用户选择和讨论记录
  候选的结构化变更

decision/
  已确认的设计决策
  Token 变更
  受影响的组件和范围

handoff/
  最终预览
  当前 Token 状态
  实现 Prompt
  给开发助手的上下文包
```

除非用户主动归档，否则不要求保存 HTML、截图或动效视频。需要重新查看时，Dockyard 可以根据当前代码、Token 和审查记录重新生成审查页面。

## 11. Dockyard 中心管理模型

Dockyard 负责管理工作流状态，但项目的长期源文件应当可以被不同设备读取和同步。推荐把项目设计资料写入用户选择的代码仓库：

```text
<project>/
  .dockyard/
    design/
      DESIGN.md
      token-schema.json
      project-tokens.json
      decisions/
      reviews/
    context/
  src/
```

其中：

- `DESIGN.md` 是项目设计规则；
- `token-schema.json` 是全局管理范围在项目中的版本化副本；
- `project-tokens.json` 是当前项目的具体 Token 值；
- `decisions/` 保存已经确认的设计决策；
- `reviews/` 保存审查编号、Token 补丁、影响范围和用户选择，不强制保存媒体；
- `.dockyard/context/` 保存发送给开发助手的上下文包。

Dockyard 本地 `userData` 只保存未提交草稿、临时 HTML、候选缓存和窗口状态。项目仓库是跨设备同步的源数据，远程 Git（版本控制）仓库负责同步和回滚。

## 12. 当前阶段的实现范围

第一版 Dockyard 工作流管理需要覆盖：

- 项目设计资料的读取和保存；
- 完整线框和项目上下文的组织；
- 模型与 Jakub、Emil 等 Skill 的调用；
- 初版 UI 和局部候选的 HTML 生成；
- 当前版本与候选版本的比较；
- 接受、拒绝和讨论；
- Token 变更和设计决策的保存；
- 发送最终上下文包给开发助手。

沿途截图和动效视频不属于必需数据。只有在用户明确需要视觉归档时，才将 HTML 或媒体作为可选附件保存。

## 13. 一句话总结

Dockyard 作为中心管理工具，使用 `DESIGN.md`、线框、Token 结构和风格 Prompt 生成一版完整 UI；之后由用户指出具体问题，模型只针对局部生成 HTML 候选和 Token 变更，用户通过真实效果进行取舍，Dockyard 只保存确认结果和结构化设计记录，不强制保存沿途媒体。
