# Dockyard 设计意图工作区

Dockyard 用原型画板、组件关联和交互关系表达设计意图，再把这些意图交给开发模型并支持后续校验。这里的领域语言描述“设计场景”本身，不描述具体实现方式。

## 设计场景

**设计场景**：一个页面或一个原型方案的完整设计上下文，包含原型结构、关系和组件选择。
_Avoid_: 项目文件、截图

**原型元素**：画板中用于表达页面结构、区域、文字或关系的可编辑元素。
_Avoid_: 图片标注、视觉稿

**设计关系**：原型元素之间表达交互或组件使用意图的明确关系。
_Avoid_: 普通箭头、连线

**交互关系**：从触发元素指向目标状态或页面的设计关系，用于表达用户操作后的去向。
_Avoid_: 组件关联

**组件关联**：把一个原型区域与组件定义连接起来的设计关系，用于表达该区域计划使用的组件。
_Avoid_: 组件实例、覆盖层组件

**组件定义**：组件来源、组件名称和变体组成的稳定引用，例如 Carbon 日期选择器的单日期变体。
_Avoid_: 静态资源编号、运行页面

**增强画稿**：仍可被 Excalidraw 打开的原生画稿，同时携带 Dockyard 的设计关系数据。
_Avoid_: 设计意图包、画板截图

**普通画稿**：只保留 Excalidraw 原生元素、不携带 Dockyard 关系数据的 `.excalidraw` 文件。
_Avoid_: 增强画稿

**设计意图摘要**：从增强画稿整理出的、面向模型快速读取的结构化摘要。
_Avoid_: 运行时状态、截图说明

**运行时预览**：根据组件关联重新加载并定位到画板上的组件显示层，不属于原生画稿元素。
_Avoid_: 保存图片、原生组件
